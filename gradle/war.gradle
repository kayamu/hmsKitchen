apply plugin: "war"

bootWar {
    mainClass = "com.polarbears.capstone.hmskitchen.HmskitchenApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
